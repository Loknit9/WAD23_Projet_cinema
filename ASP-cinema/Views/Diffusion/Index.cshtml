@model IEnumerable<ASP_cinema.Models.Diffusion.DiffusionListItemViewModel>

@{
    ViewData["Title"] = "Diffusion";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id_Diffusion }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id_Diffusion }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id_Diffusion })
            </td>
        </tr>
}
    </tbody>
</table>
